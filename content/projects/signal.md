Human in the loop dispatch software for wildfire surge events. Four agents triage calls, route the nearest unit, and voice a briefing, under a strict rule that AI never speaks to a caller directly.

I built the entire real-time dashboard: auto-reconnecting WebSocket state over eight message types, a Leaflet map with live fire perimeter, override and hold gates, plus live call transcription and a phone-in voice agent.
