Context window management for AI coding agents: fold, unfold, and pin any part of an agent's context without ever deleting it.

I built a WebSocket based conductor, an alternative automatic folding strategy, then returned later to ship the system prompt as a first class, non foldable dial: capture hook, token accounting, wire protocol bump, new UI panel, 746 tests passing. Also built the Windows release installer workflow.
